<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Verified Successfully</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #f5f5f5;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .success-card {
            background: white;
            padding: 3rem;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            text-align: center;
            max-width: 500px;
            width: 90%;
        }

        .checkmark {
            font-size: 4rem;
            color: #1DC085;
            margin-bottom: 1.5rem;
        }

        h1 {
            color: #1DC085;
            margin-bottom: 1rem;
        }

        p {
            color: #666;
            line-height: 1.6;
            margin-bottom: 2rem;
        }

        .login-btn {
            background: #1DC085;
            color: white;
            padding: 12px 35px;
            border: none;
            border-radius: 25px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .login-btn:hover {
            background: #17a076;
            transform: translateY(-2px);
        }

        .security-tip {
            margin-top: 2rem;
            font-size: 0.9rem;
            color: #888;
        }
    </style>
</head>
<body>
    <div class="success-card">
        <div class="checkmark">✓</div>
        <h1>Email Verified Successfully!</h1>
        <p>Your email <strong><%= userEmail %></strong> has been confirmed. You can now access your account and start using our services.</p>
        
        <a href="<%= loginPageUrl %>" class="login-btn">Continue to Login</a>

        <div class="security-tip">
            <p>🔒 Always keep your login credentials secure</p>
        </div>
    </div>
</body>
</html>